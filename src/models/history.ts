/** History. */
export type History = {
    /** Id. */
    readonly id: number;
    /** Date. */
    readonly date: Date;
    /** Command. */
    readonly command: string;
    /** Output. */
    readonly output: string;
};
